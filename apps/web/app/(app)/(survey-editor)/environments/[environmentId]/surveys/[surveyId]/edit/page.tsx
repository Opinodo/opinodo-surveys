import { SurveyEditorPage } from "@/modules/survey/editor/page";

// Increase timeout for long-running translation requests with many languages
export const maxDuration = 20; // seconds

export default SurveyEditorPage;
